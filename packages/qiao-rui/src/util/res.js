/**
 * grid
 * @param {*} xl
 * @param {*} lg
 * @param {*} md
 * @param {*} sm
 * @returns
 */
export const grid = (xl, lg, md, sm, d) => {
  return `pure-u-x4k-${xl}-24 pure-u-xxxl-${xl}-24 pure-u-xxl-${xl}-24 pure-u-xl-${xl}-24 pure-u-lg-${lg}-24 pure-u-md-${md}-24 pure-u-sm-${sm}-24 pure-u-${d}-24`;
};
